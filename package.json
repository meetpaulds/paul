{
  "name": "paul",
  "version": "1.3.3",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter paul-storybook-react dev",
    "dev:react": "pnpm --filter paul-storybook-react dev",
    "dev:vue": "pnpm --filter paul-storybook-vue dev",
    "dev:svelte": "pnpm --filter paul-storybook-svelte dev",
    "dev:angular": "pnpm --filter paul-storybook-angular dev",
    "dev:storybook": "turbo run build --filter=@meetpaul/tokens && concurrently \"pnpm --filter paul-storybook-react dev\" \"pnpm --filter paul-storybook-vue dev\" \"pnpm --filter paul-storybook-svelte dev\" \"pnpm --filter paul-storybook-angular dev\"",
    "build": "turbo run build --filter=\"./packages/*\"",
    "build:all": "turbo run build",
    "build:storybook": "turbo run build --filter=\"paul-storybook-*\"",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test --filter=\"./packages/*\"",
    "clean": "turbo run clean && rm -rf node_modules",
    "version:patch": "pnpm -r --filter \"./packages/*\" exec npm version patch --no-git-tag-version && npm version patch --no-git-tag-version",
    "version:minor": "pnpm -r --filter \"./packages/*\" exec npm version minor --no-git-tag-version && npm version minor --no-git-tag-version",
    "version:major": "pnpm -r --filter \"./packages/*\" exec npm version major --no-git-tag-version && npm version major --no-git-tag-version",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@typescript-eslint/eslint-plugin": "^8.60.0",
    "@typescript-eslint/parser": "^8.60.0",
    "concurrently": "^9.0.0",
    "eslint": "^10.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "prettier": "^3.8.3",
    "turbo": "^2.9.14",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@9.0.0"
}
